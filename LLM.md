# zap (TypeScript) — ZAP zero-copy app proto

TypeScript/JavaScript ZAP implementation. Full docs in README.md.

npm: published as **`@zap-proto/zap`**. NOTE: `package.json` currently declares the
name `@zap-protocol/zap` — reconcile to the canonical `@zap-proto/zap` scope.
